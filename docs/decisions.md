# 意思決定の記録（承認・却下の履歴）

> **却下理由は必ず1行書くこと。この蓄積が次回の提案精度を決める。**
>
> 「なんとなく違う」でもよいので言語化する。理由が無い却下は、同じ提案を何度も
> 生むだけで誰の得にもならない。承認したものも、後から「なぜ入れたか」を辿れるように残す。

## 書き方

| 日付 | 提案内容 | 判断 | 理由 |
|---|---|---|---|
| YYYY-MM-DD | 何を変える提案か（1行） | 承認 / 却下 / 保留 | なぜそう判断したか（1行以上・必須） |

- 提案の詳細は `docs/proposals/` に置き、この表からはファイル名で参照する。
- 「保留」は条件付き。何が揃えば再検討するかを理由欄に書く。
- 足場設計の絶対原則（建物と足場は平行／1面内・同一面の全階で離れ一定）に反する提案は、
  他がどれだけ良くても却下。理由欄には「原則に反するため」と、どの原則かを書く。

## 履歴

| 日付 | 提案内容 | 判断 | 理由 |
|---|---|---|---|
| 2026-08-06 | フェーズ0: 行動計測基盤（events テーブル・lib/analytics.ts・集計スクリプト）を追加 | 承認 | 事実の裏付けなしに機能の追加・廃止を判断していたため。計測は非ブロッキングで既存動作に影響しない |
| 2026-08-06 | events の insert を anon にも許可する | 却下 | 本サービスは全機能がログイン必須で、匿名で得られるのはログイン画面の表示だけ。そのために誰でも書き込める口を開ける割に合わない（authenticated のみに変更） |
| 2026-08-06 | events に DB トリガでレート制限を入れる | 却下 | 書き込みのたびに count() が走り、計測のために本体の書き込みが重くなる。入口を authenticated に絞った時点で匿名の流し込みは塞がっているため、送る側の上限（1 セッション 1000 件）で足りる |
| 2026-08-06 | プライバシーポリシーに「利用状況の記録」を追記（保存期間 180 日・第三者提供なし） | 承認 | 記録を始める以上、何を取り何を取らないかを利用者に明示する必要があるため。文面は高齢の利用者に合わせて平易にした |
| 2026-08-06 | 計測キューを sessionStorage に保存する（localStorage / Cookie は不採用） | 承認 | ページ遷移でモジュール変数が消え、ログイン画面のイベントが実測で全滅したため。sessionStorage はタブの中だけで生き、session_id と寿命が一致する。localStorage は別タブ・翌日まで古いログが残り、いつのものか分からなくなる |
| 2026-08-06 | 匿名ハッシュの元を「Supabase セッションの user.id」1 つに限定（仮 id は使わない） | 承認 | 実測で 1 セッションに 2 つのハッシュが付いていた。原因はストアの初期値 'anonymous' をハッシュしていたこと。同一人物が同一値になることが利用者数の前提 |
